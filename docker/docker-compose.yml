version: "3.8"
services:
  sandbox:
    build:
      context: .
      dockerfile: Dockerfile.sandbox
    image: sandobox-base:latest
    ports:
      - "4096"
    volumes:
      - sandbox-data:/workspace
    environment:
      - OPENCODE_SERVER_HOSTNAME=0.0.0.0

volumes:
  sandbox-data:
